# 핵심 (MCP 서버 + 로컬 벡터)
mcp>=1.2.0
sqlite-vec>=0.1.9
pyyaml>=6.0
jsonschema>=4.0

# 로컬 임베딩 (기본 BAAI/bge-m3, 대안 Qwen/Qwen3-Embedding-0.6B). 미설치 시 hash 폴백(테스트용).
sentence-transformers>=3.0

# 멀티프로바이더 추론 실행층 (tools/lib/llm.py). 키/모델 명시 시에만 필요 — 선택 설치.
litellm>=1.40

# 인제스트 추출 (data/update 의 다양한 포맷 -> 텍스트)
pypdf>=4.0            # .pdf
python-docx>=1.1      # .docx
beautifulsoup4>=4.12  # .html
openpyxl>=3.1         # .xlsx/.xlsm (시트별 표; values_only 로 실제 셀 값 추출)
pillow>=10.0          # 이미지 로드
pytesseract>=0.3      # 이미지 OCR — 별도로 tesseract 바이너리 설치 필요
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   macOS:   brew install tesseract   |   Ubuntu: apt install tesseract-ocr
