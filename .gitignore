# secrets — 절대 커밋 금지
.env
*.key
*.pem
**/secrets/**
**/credential*.local.*

# 파이썬 venv (대용량 — 재생성 가능)
.venv/

# 파생물(재생성 가능) — 벡터 바이너리/임베딩
**/info/vector/*.bin
**/info/vector/*.faiss
**/info/vector/*.lance/
**/info/vector/*.db
**/info/vector/*.db-journal

# 진입 규칙 파일: 정본 AGENTS.md 만 추적. 나머지는 정본으로의 심링크(미추적,
# `harness gen-rules`/`make ready`가 로컬 생성; 심링크 미지원 OS면 복제 폴백).
CLAUDE.md
GEMINI.md
.cursorrules
**/copilot-instructions.md
**/.cursor/rules/workspace.mdc

# 상태/락
**/state/lock.json

# 일반
__pycache__/
*.pyc
.DS_Store
node_modules/
**/*.sqlite-journal
**/*.db-journal
